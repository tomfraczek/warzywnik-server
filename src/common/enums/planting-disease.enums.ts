export enum PlantingDiseaseStatus {
  SUSPECTED = 'suspected',
  CONFIRMED = 'confirmed',
  RESOLVED = 'resolved',
}

export enum PlantingDiseaseSeverity {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}
