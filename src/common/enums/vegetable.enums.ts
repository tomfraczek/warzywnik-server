export enum PlantType {
  ANNUAL = 'annual',
  PERENNIAL = 'perennial',
  BIENNIAL = 'biennial',
}

export enum GrowthForm {
  BUSH = 'bush',
  VINE = 'vine',
  TREE = 'tree',
  TUBE = 'tuber',
}

export enum LifeCycle {
  ANNUAL = 'annual',
  PERENNIAL = 'perennial',
  BIENNIAL = 'biennial',
}

export enum FrostResistance {
  NONE = 'none',
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}

export enum SunExposure {
  FULL_SUN = 'full_sun',
  PARTIAL_SHADE = 'partial_shade',
  SHADE = 'shade',
}

export enum WaterNeeds {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}

export enum NutrientNeeds {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}

export enum WindowType {
  SOWING = 'sowing',
  TRANSPLANTING = 'transplanting',
  PLANTING_OUT = 'planting_out',
  HARVEST = 'harvest',
}

export enum MediaType {
  IMAGE = 'image',
  VIDEO = 'video',
  ILLUSTRATION = 'illustration',
}

export enum CompanionRelation {
  GOOD = 'good',
  BAD = 'bad',
  ALLELOPATHIC = 'allelopathic',
}

export enum RotationRelation {
  GOOD_AFTER = 'good_after',
  NOT_AFTER = 'not_after',
}

export enum DifficultyLevel {
  EASY = 'easy',
  MEDIUM = 'medium',
  HARD = 'hard',
}

export enum HarvestFrequency {
  ONCE = 'once',
  MULTIPLE = 'multiple',
}
