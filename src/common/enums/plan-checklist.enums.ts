export enum PlanChecklistScope {
  BED = 'bed',
  PLANTING = 'planting',
}

export enum PlanChecklistStatus {
  PENDING = 'pending',
  DONE = 'done',
  SKIPPED = 'skipped',
}

export enum PlanChecklistSource {
  AUTO = 'auto',
  MANUAL = 'manual',
}

export enum PlanChecklistPriority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}
