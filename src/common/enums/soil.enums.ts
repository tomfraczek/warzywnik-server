export enum SoilType {
  LIGHT = 'light',
  MEDIUM = 'medium',
  HEAVY = 'heavy',
}

export enum SoilStructure {
  LOOSE = 'loose',
  CRUMBLY = 'crumbly',
  COMPACT = 'compact',
}

export enum DemandLevel {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}

export enum DrainageLevel {
  POOR = 'poor',
  MEDIUM = 'medium',
  GOOD = 'good',
}
